/*
-Variables are named containers for storing data values
-We name variables so we can refer to them and the values at a later time
*/

let a = 2;

/*
(1)  (2)    (3)  (4)
let   b      =    2

1.  JS keyword to denote the creation of a variable
2.  The name of the variable in which the dev refers to in order to grab the value stored
3.  Assisngment operator
4.  The intial value
*/

//The declaration is to the left of the assignment operator(=)
//This is where the variable os created by using a designated JS keyword (var, let, const) and giving it a name

let s;


//The intitialization is to the right side of the assignment operator(=)
//This wher the dev sets the value for the declared variable
//      (1)
let x = 10;

//1 is the Intialization


let y;
console.log("Declaration", y)

y = 10;
console.log("Initialization", y);

y = 35
console.log("Initialization 2", y);

